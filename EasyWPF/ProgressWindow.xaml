<Window x:Class="EasyWPF.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Progression de la sauvegarde" Height="200" Width="400">
    <Grid>

<Button   Command="{Binding RunSelectedBackupCommand}" />
        <!-- <TextBlock  Text="{Binding ProgressText, StringFormat=Progression : {0}%}" Panel.ZIndex="50"  HorizontalAlignment="Left" Margin="600,230,0,0" TextWrapping="Wrap" VerticalAlignment="Top" FontSize="20" Foreground="White"></TextBlock>

        <ProgressBar  Panel.ZIndex="50"   Minimum="0" Maximum="100" Value="{Binding ProgressText}" Height="30" Margin="150,0,0,0" Width="300" HorizontalAlignment="Center" VerticalAlignment="Center" Background= "#3B1C32" Foreground="#A64D79" />
        <Button   Panel.ZIndex="20"  Foreground="#FF210F22" HorizontalAlignment = "Center" VerticalAlignment = "Center"  Margin="150,100,0,0" Content="{Binding PlayPauseButtonText}" Style="{StaticResource ButtonWithOutHover2}"  Background= "#A64D79" /> -->





        <!-- Conteneur pour les boutons -->
        <!-- <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center" />
        
    <DataGrid Opacity="1" x:Name="Datagrid" ItemsSource="{Binding Backups}" GridLinesVisibility="None" Foreground="#FF210F22"         Background="#FFD1A1C7"
  

          RowBackground="#2C0A12" Width="504" Height="180"  HorizontalAlignment="Center" VerticalAlignment="Center" AutoGenerateColumns="False" Panel.ZIndex="3">
    
    <DataGrid.Style>
        <Style TargetType="DataGrid">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Items.Count, RelativeSource={RelativeSource Self}}"
                             Value="0">
                    <Setter Property="Background" Value="LightGray"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </DataGrid.Style>
    <DataGrid.Resources>
      
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/> 
            <Setter Property="FontWeight" Value="Bold"/> 
        </Style>
        
    </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTemplateColumn  x:Name ="DatagridBorder2Header5"  Header="Choix" Width="40"   >
                    <DataGridTemplateColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Background" Value="#FFD1A1C7"/>
                        </Style>
                    </DataGridTemplateColumn.CellStyle>
                    
                    <DataGridTemplateColumn.CellTemplate  >
                        <DataTemplate >
                            <CheckBox        Background="#FFD1A1C7"               HorizontalAlignment="Center" 
                      VerticalAlignment="Center" IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="25"  />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
 

                </DataGridTemplateColumn>
                <DataGridTextColumn x:Name ="DatagridBorder2Header1"  Header="Nom" Binding="{Binding Name}" Width="75"  >
        <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock">
                <Setter Property="Background" Value="#FFD1A1C7"/>
            </Style>
        </DataGridTextColumn.ElementStyle>
    </DataGridTextColumn>
                <DataGridTextColumn x:Name ="DatagridBorder2Header2" Header="Source" Binding="{Binding SourcePath}" Width="150"  >
        <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock">
                <Setter Property="Background" Value="#FFD1A1C7"/>
            </Style>
        </DataGridTextColumn.ElementStyle>
    </DataGridTextColumn>
                <DataGridTextColumn x:Name ="DatagridBorder2Header3" Header="Destination" Binding="{Binding DestinationPath}" Width="150"  >
        <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock">
                <Setter Property="Background" Value="#FFD1A1C7"/>
            </Style>
        </DataGridTextColumn.ElementStyle>
    </DataGridTextColumn>
                <DataGridTextColumn x:Name ="DatagridBorder2Header4" Header="Type" Binding="{Binding BackupType}" Width="75" >
        <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock">
                <Setter Property="Background" Value="#FFD1A1C7"/>
            </Style>
        </DataGridTextColumn.ElementStyle>
    </DataGridTextColumn>
                
            </DataGrid.Columns>
        </DataGrid> -->
    </Grid>
</Window>
