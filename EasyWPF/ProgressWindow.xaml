<Window x:Class="EasyWPF.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="Progression de la sauvegarde" Height="200" Width="400">

<Grid>
        
<!-- <StackPanel Orientation="Vertical">
    <TextBlock  Text="{Binding ProgressText, StringFormat=Progression : {0}%}"   TextWrapping="Wrap"  FontSize="20" Foreground="Black"></TextBlock>
    <ProgressBar  Minimum="0" Maximum="100" Value="{Binding ProgressText}" Height="30" Width="300"  Background= "#3B1C32" Foreground="#A64D79" />
    <Button Content="Démarrer" Command="{Binding StartCommand}" Width="100" Height="30" Margin="5" />
    <Button Content="Arrêter" Command="{Binding StopCommand}" Width="100" Height="30" Margin="5" />
    <TextBlock Text="{Binding Boolrun}" />

</StackPanel> -->


 <DataGrid ItemsSource="{Binding EncoursBackups}" AutoGenerateColumns="False" >
    <DataGrid.Columns>
        <DataGridTextColumn Header="ID" Binding="{Binding ID}" />
        <DataGridTextColumn Header="ID" Binding="{Binding Name}" />
        <DataGridTextColumn Header="Progression" Binding="{Binding Progress, StringFormat=Progression : {0}%}" />
    <!-- <ProgressBar  Minimum="0" Maximum="100" Value="{Binding Progress}" Height="30" Width="300"  Background= "#3B1C32" Foreground="#A64D79" /> -->
        <!-- <DataGridTemplateColumn Header="Progression">
            <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                    <ProgressBar Minimum="0" Maximum="100" Value="{Binding Progress}" Height="30" Width="300" Background="#3B1C32" Foreground="#A64D79" />
                </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn> -->      
         <DataGridTemplateColumn Header="Actions">
            <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                    <StackPanel >
                        <Button Content="Play" Command="{Binding DataContext.PlayCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding ID}" />
                        <Button Content="Stop" Command="{Binding DataContext.StopCommand2, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding ID}" />
                    </StackPanel>
                </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
    </DataGrid.Columns>
</DataGrid> 
<!-- 
         <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center" />
        
    <DataGrid  x:Name="Datagrid" ItemsSource="{Binding Backups}" GridLinesVisibility="None" Foreground="#FF210F22"         Background="#FFD1A1C7"
  

          RowBackground="#2C0A12" Width="504" Height="180"  HorizontalAlignment="Center" VerticalAlignment="Center" AutoGenerateColumns="False" Panel.ZIndex="3">
    
    <DataGrid.Style>
        <Style TargetType="DataGrid">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Items.Count, RelativeSource={RelativeSource Self}}"
                             Value="0">
                    <Setter Property="Background" Value="LightGray"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </DataGrid.Style>


            <DataGrid.Columns>

                <DataGridTextColumn   Header="Nom" Binding="{Binding Name}" Width="75"  >
        <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock">
                <Setter Property="Background" Value="#FFD1A1C7"/>
            </Style>
        </DataGridTextColumn.ElementStyle>
    </DataGridTextColumn>
                <DataGridTextColumn Header="Progression" Binding="{Binding Progression}" Width="150"  >
        <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock">
                <Setter Property="Background" Value="#FFD1A1C7"/>
            </Style>
        </DataGridTextColumn.ElementStyle>
    </DataGridTextColumn>
                <DataGridTextColumn Header="BoolRun" Binding="{Binding BoolRun}" Width="150"  >
        <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock">
                <Setter Property="Background" Value="#FFD1A1C7"/>
            </Style>
        </DataGridTextColumn.ElementStyle>
    </DataGridTextColumn>
            <DataGridTemplateColumn Header="Action">
            <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                    <Button Content="Run" Command="{Binding RunSelectedBackupCommand}" CommandParameter="{Binding}" />
                </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
    </DataGrid.Columns>
 -->


        <!-- <DataGrid ItemsSource="{Binding SelectedBackups}" AutoGenerateColumns="False" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Progress" Binding="{Binding Progression}" Width="*"/>
                <DataGridTemplateColumn Header="Actions" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="▶" Width="30" Height="30" Margin="5" Command="{Binding DataContext.StartBackupCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                                <Button Content="⏹" Width="30" Height="30" Margin="5" Command="{Binding DataContext.StopBackupCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid> -->





    </Grid>
</Window>
